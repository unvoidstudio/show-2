So we can build a spaceship on stream.
We can use a 3D-program or build it from toilet roles and glue.
Then we take pictures of it and use it as background when we stream, so it looks like we are inside it.
